{# @var plugin \agenceink\craftmailerlite\Plugin #}
{# @var settings \agenceink\craftmailerlite\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.autosuggestField ({
    first: true,
    label: "Enter your Mailerlite API key:",
    name: "apiKey",
    value: settings.apiKey,
    suggestEnvVars: true,
    required:true,
}) }}

{{ forms.textField({ 
    label: "Subscribe users to the following groups:",
    instructions: "Please fill this field with a group id. You can use commas to specify multiple groups id.",
    name: "groups",
    value: settings.groups,
 }) }}

{{ forms.lightswitchField({
    label: "Auto-subscribe users on website registration:",
    name: "registration", 
    on: settings.registration,
}) }}
